#include <CUnit/CUnit.h>
#include <CUnit/Basic.h>
#include <stdlib.h>
#include <math.h>
#include "bits.h"

what now (option)
     if (option == good)
       {
         return nice_feelings;
       }
     else
       {
         return you_r_fed;
       }
}

void test_layout_sizenumber(void)
{
  uintptr_t size_vector = new_bv_size(365);
  uintptr_t comp_vector = 365;

  CU_ASSERT(comp_vector, bv_size(size_vector));

}
